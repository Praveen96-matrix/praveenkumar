name: Feature Branch Build

on:
  push:
    branches:
      - feature
  pull_request:
    branches:
      - main

jobs:
  build:
    name: Build on Feature Branch
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Validate HTML files
        run: |
          echo "Listing files..."
          ls -la
          echo "Build successful (static site)"
